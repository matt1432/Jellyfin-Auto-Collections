[tool.basedpyright]
allowedUntypedLibraries = ["apscheduler", "pluginlib", "pyaml_env"]
reportAny = false

[tool.ruff]
target-version = "py313"
line-length = 80
lint.select = ["E", "F", "I", "U"]
lint.ignore = ["E501"]
